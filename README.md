# USAA — Universal Smart Assistant Agent (prototype)

Run:
1. (optional) create & activate venv
2. pip install -r requirements.txt
3. python src/main.py

Notes:
- This repo uses mock engine wrappers. Replace `generate()` implementations in src/engines/* to call real APIs.
- Add keys to `.env` or environment variables before enabling real APIs.
